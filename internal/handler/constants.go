package handler

const (
	// DefaultLimit is the default pagination limit for list endpoints.
	DefaultLimit = 50

	// MaxLimit is the maximum pagination limit.
	MaxLimit = 200

	// DefaultAdminActionsLimit is the default limit for admin action logs.
	DefaultAdminActionsLimit = 100
)
